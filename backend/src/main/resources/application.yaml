spring:
  datasource:
    url: jdbc:mysql://localhost:3306/project_piec1?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: sua-senha-aqui

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

api:
  security:
    token:
      secret: ${JWT_SECRET:minha_chave_super_secreta_para_gerar_tokens_123}

# Adicionar no application.yaml abaixo das configurações existentes

mqtt:
  broker: tcp://broker.hivemq.com:1883
  clientId: spring-hub-ioe-backend
  topic: hubioe/residencia/leituras
  dispositivoEnergiaId: 1
  dispositivoAguaId: 2
