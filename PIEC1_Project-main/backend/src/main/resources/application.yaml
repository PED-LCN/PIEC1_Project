spring:
  properties:
    name: project-piec1

  datasource:
    url: jdbc:mysql://localhost:3306/project_piec1?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: julio
    password: Jug1310. 

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration


